# Discord-Music-Bot 0508 改善企劃書

根據 2026-05-08 的程式碼審查結果，針對現有的音樂機器人架構提出以下優化建議，旨在提升系統穩定性、資源利用率及使用者體驗。

## 1. 系統穩定性優化 (Reliability)

### A. 優雅的進程清理 (Graceful Cleanup)
- **現狀**：目前在 `audioPipeline.js` 中直接使用 `SIGKILL` 殺死 `yt-dlp` 與 `ffmpeg` 進程。
- **改進**：實施「先禮後兵」策略。
    1. 先發送 `SIGTERM` 允許進程完成緩衝區寫入或正常關閉。
    2. 設定 2-3 秒的超時 (Timeout)。
    3. 若進程仍未結束，再發送 `SIGKILL` 強制結束。
- **目標**：防止在極端情況下產生殭屍進程或資源洩漏。

### B. 面板競爭條件防護 (Dashboard Lock)
- **現狀**：當多個事件（如指令點歌與自動播放切換）同時發生時，可能導致面板重複發送或刪除失敗。
- **改進**：在 `GuildPlayer` 類別中引入 `_isResending` 鎖（Mutex 概念）。
    - 確保 `resendDashboard` 在同一時間只有一個實例在執行。
    - 在刪除與發送新訊息之間加入更嚴謹的狀態檢查。

## 2. 效能與資源優化 (Performance)

### A. 語音權限預檢
- **建議**：在 `GuildPlayer.js` 的 `resendDashboard` 中，增加對 `Manage Messages` 權限的檢查。
- **理由**：若機器人被邀請至權限受限的頻道，頻繁的 `.delete()` 失敗會拋出 API 錯誤，增加系統負擔。

### B. ffmpeg 參數微調
- **建議**：針對 `e2-micro` 等低配環境，可在 `ffmpeg` 參數中加入 `-threads 1`，避免瞬間多線程運算導致 CPU 暴漲觸發 GCP 的流量限制。

## 3. 使用者體驗優化 (UX)

### A. 智慧電台推薦邏輯
- **現狀**：目前是隨機從歷史紀錄挑選。
- **未來改進**：利用 `yt-search` 抓取當前歌曲的相關影片 (Related Videos)，實現更精準的「自動續播」功能。

### B. 指令響應一致性
- **改進**：將所有按鈕操作（如暫停/恢復）的 UI 更新邏輯與 `resendDashboard` 整合，確保面板始終保持在頻道最底部，方便行動裝置使用者操作。

---

## 4. 待處理清單 (Action Items)

- [ ] 修改 `audioPipeline.js` 的 `cleanup` 邏輯。
- [ ] 在 `GuildPlayer` 實作 Dashboard Mutex。
- [ ] 測試在無「管理訊息」權限下的報測處理。
- [ ] (選配) 整合 YouTube 關聯影片 API 至 Radio Mode。
